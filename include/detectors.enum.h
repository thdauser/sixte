#ifndef DETECTORS_ENUM_H
#define DETECTORS_ENUM_H 1

/** Specification index for the different detector types. */
typedef enum {
  FRAMESTORE=1, 
  DEPFET    =2, 
  TES       =3,
  HTRS      =4
} DetectorTypes;

#endif /* DETECTORS_ENUM_H */
